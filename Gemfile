source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in image_blur.gemspec
gem 'rspec'
gem 'guard-rspec', require: false
gem 'pry-nav'
